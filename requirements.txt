# Core analysis pipeline
pandas>=2.0
numpy>=1.24
scipy>=1.10
statsmodels>=0.14
matplotlib>=3.7
Pillow>=10.0

# HPAP CellxGene access (notebook only)
httpx>=0.24
tenacity>=8.0
tqdm>=4.65
pyarrow>=12.0

# FBS decoder for HPAP expression payloads (notebook only)
# Install with: pip install --no-deps cellxgene flatbuffers
# (--no-deps because the cellxgene package pulls a heavy server stack
# that isn't needed for the decoder we use)
# cellxgene
# flatbuffers
