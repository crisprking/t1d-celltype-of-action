# Pipeline runtime dependencies. For development install, prefer:
#   pip install -e ".[dev]"
pandas>=2.0
numpy>=1.24
scipy>=1.10
statsmodels>=0.14
matplotlib>=3.7
Pillow>=10.0
httpx>=0.27
tenacity>=8.0
tqdm>=4.65
pyarrow>=12.0

# Optional: HPAP CellxGene FBS decoder.
# Install with: pip install --no-deps cellxgene flatbuffers
# (--no-deps avoids the heavy server stack that we don't use.)
